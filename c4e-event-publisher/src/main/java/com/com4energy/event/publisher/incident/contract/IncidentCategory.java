package com.com4energy.event.publisher.incident.contract;

/** Clasificacion funcional del incidente (que paso). */
public enum IncidentCategory {
    APPLICATION,
    FILE_PROCESSING,
    INTEGRATION,
    VALIDATION,
    SECURITY,
    SYSTEM
}


services:
  records-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: records-api
    env_file:
      - .env
    ports:
      - "8082:8082"
    restart: unless-stopped
